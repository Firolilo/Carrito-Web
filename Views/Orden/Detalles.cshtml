@model CarritoDeComprasMVC.Models.Entity.Orden

<div class="container mt-5">
    <h2 class="text-center">Detalles de la Orden</h2>
    <p class="text-muted text-center">Fecha de la compra: @Model.FechaCreacion.ToString("g")</p>

    <div class="table-responsive">
        <table class="table table-striped table-hover align-middle">
            <thead class="table-dark">
                <tr>
                    <th scope="col">Producto</th>
                    <th scope="col" class="text-center">Cantidad</th>
                    <th scope="col" class="text-center">Precio Unitario</th>
                    <th scope="col" class="text-center">Total</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Items)
                {
                    <tr>
                        <td>@item.Producto.Nombre</td>
                        <td class="text-center">@item.Cantidad</td>
                        <td class="text-center">$@item.PrecioUnitario.ToString("F2")</td>
                        <td class="text-center">$@item.PrecioTotal.ToString("F2")</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <h3 class="text-end text-primary">Total de la Orden: $@Model.Total.ToString("F2")</h3>
</div>
